<project>
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.cal2crm</groupId>
    <artifactId>cal2crm-platform</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
            <optional>true</optional>
        </dependency>
    </dependencies>

    <!-- Dependency management for all modules -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.cal2crm.core</groupId>
                <artifactId>core-parent</artifactId>
                <version>${project.version}</version>
                <type>pom</type>
            </dependency>
            <dependency>
                <groupId>com.cal2crm.storage</groupId>
                <artifactId>storage-parent</artifactId>
                <version>${project.version}</version>
                <type>pom</type>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <modules>
        <module>core/core-api</module>
        <module>core/core-spi</module>
        <module>core/core-engine</module>
        <module>core/canonical-api</module>
        <module>core/canonical-model</module>
        <module>core/canonical-mapping</module>
        <module>core/unified-api</module>
        <module>core/unified-model</module>
        <module>storage/storage-api</module>
        <module>storage/storage-postgres</module>
    </modules>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.11.0</version>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
